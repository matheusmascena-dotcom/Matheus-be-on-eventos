name: Legacy site enhancements (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy visual workflow disabled. Final site changes are maintained by the consolidated presentation workflow."